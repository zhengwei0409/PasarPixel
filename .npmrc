# Tolerate slow networks: retry harder and wait longer before timing out.
# Prevents partial downloads of large packages (e.g. prisma's query engine).
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=600000
network-concurrency=4
